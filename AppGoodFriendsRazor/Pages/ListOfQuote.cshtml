@page
@model AppGoodFriendsRazor.Pages.ListOfQuoteModel 
@{
    ViewData["Title"] = "List of famous quotes";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@attribute [IgnoreAntiforgeryToken] 

<!-- Page List of quotes -->
<div class="container px-4 py-4" id="list-of-quotes">
    <h2 class="pb-2 border-bottom">List of famous quotes</h2>

    <p>Below are some famous quotes about love, work and the fact that tomorrow is another day.</p>

    <div class="row row-cols-1 row-cols-lg-4 align-items-stretch g-4 py-5">
        <div class="col-md-7 col-lg-10">
            <div class="row mb-2 text-center">
                <div class="col-md-10 themed-grid-head-col">Group Name</div>
                <div class="col-md-2 themed-grid-head-col">
                    <a href="~/Edit/EditQuote" class="btn btn-success btn-sm m-1" type="button">New</a>
                </div>
            </div>
            <form method="post" class="needs-validation" novalidate>
                <div class="row mb-2 text-center">
                    
                    @if (Model.QuoteList is not null)
                    {
                        <!-- This is where selected item id will be stored -->
                        <input type="hidden" name="quoteId" data-seido-selected-item-id-target />

                        @foreach (var item in Model.QuoteList)
                        {
                            <div class="col-md-10 themed-grid-col">
                                <a href="~/Edit/AddEditQuote?id=@item.QuoteId">
                                    @item
                                </a>
                            </div>
                            <div class="col-md-2 themed-grid-col">
                                <a href="~/Edit/AddEditQuote?id=@item.QuoteId" class="btn btn-secondary btn-sm m-1" type="button">Edit</a>

                                <!-- Button to trigger modal confirmation with modal in the form tag -->
                                <button type="button" class="btn btn-danger btn-sm m-1" data-bs-toggle="modal" data-bs-target="#delModal"
                                        data-seido-modal-body="@item.Quote is about to be deleted."
                                        data-seido-selected-item-id="@item.QuoteId">
                                    Del
                                </button>
                            </div>
                        }
                    }

                    <!-- Modal in the form tag -->
                    <div class="modal fade" id="delModal" tabindex="-1" aria-labelledby="softModalLabel" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title text-danger" id="softModalLabel">Confirm deletion</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    Hello from modal
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                                    <button type="submit" asp-page-handler="DeleteGroup" class="btn btn-primary btn-danger">Ok</button>
                                </div>
                            </div>
                        </div>
                    </div>
                    
                </div>
            </form>
        </div>
    </div>
</div>
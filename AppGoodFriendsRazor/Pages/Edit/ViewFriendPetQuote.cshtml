@page
@model AppGoodFriendsRazor.Pages.Edit.ViewFriendPetQuoteModel
@{
    ViewData["Title"] = "View and Edit details of a friend including address, pets, and quotes";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@attribute [IgnoreAntiforgeryToken]

<div class="container px-4 py-4" id="view-friend-pet-quote">
    <h2 class="pb-2 border-bottom">View details of a friend</h2>
    <p>Below you find all the details of my friend. You can also modify the pets my friend has and the quotes he/she likes.</p>

    <div class="row row-cols-1 row-cols-lg-4 align-items-stretch g-4 py-5">
        <div class="col-md-7 col-lg-8">
            <form class="needs-validation" novalidate>
                <div class="row g-3">
                    
                    <!-- Hidden fields used to bind input model -->
                    <input asp-for="ViewFriendIM.FriendId" type="hidden" />
                    <input asp-for="ViewFriendIM.StatusIM" type="hidden" />
                    <input asp-for="ViewFriendIM.Pets" type="hidden" />

                    
                    <div class="col-sm-6">
                        <label for="firstName" class="form-label">First name</label>
                        <input type="text" class="form-control" id="firstName" asp-for="ViewFriendIM.Firstname"> 
                        <span asp-validation-for="ViewFriendIM.Firstname" class="invalid-feedback"></span>
                    </div>

                    <div class="col-sm-6">
                        <label for="lastName" class="form-label">Last name</label>
                        <input type="text" class="form-control" id="lastName" asp-for="ViewFriendIM.Lastname">
                        <span asp-validation-for="ViewFriendIM.Lastname" class="invalid-feedback"></span>
                    </div>

                    <div class="col-sm-6">
                        <label for="email" class="form-label">Email</label>
                        <input type="email" class="form-control" id="email" asp-for="ViewFriendIM.Email">
                    </div>
                    <div class="col-sm-6">
                        <label for="birthday" class="form-label">Birthday</label>
                        <input type="birthday" class="form-control" id="birthday" asp-for="ViewFriendIM.Birthday">
                    </div>

                    <div class="row g-3 visible">
                        <div class="col-12">
                            <label for="address" class="form-label">Address</label>
                            <input type="text" class="form-control" id="address" asp-for="ViewFriendIM.AddressIM.StreetAddress">
                            <span asp-validation-for="ViewFriendIM.AddressIM.StreetAddress" class="invalid-feedback"></span>
                        </div>

                        <div class="col-md-3">
                            <label for="zip" class="form-label">Zipcode</label>
                            <input type="text" class="form-control" id="zipCode" asp-for="ViewFriendIM.AddressIM.ZipCode">
                            <span asp-validation-for="ViewFriendIM.AddressIM.ZipCode" class="invalid-feedback"></span>
                        </div>

                        <div class="col-md-4">
                            <label for="city" class="form-label">City</label>
                            <input type="text" class="form-control" id="city" asp-for="ViewFriendIM.AddressIM.City">
                            <span asp-validation-for="ViewFriendIM.AddressIM.City" class="invalid-feedback"></span>
                        </div>

                        <div class="col-md-5">
                            <label for="country" class="form-label">Country</label>
                            <input type="text" class="form-control" id="country" asp-for="ViewFriendIM.AddressIM.Country">
                        </div>
                    </div>
                </div>

                <a href="~/Edit/AddEditFriend?id=@Model.ViewFriendIM.FriendId" class="w-100 btn btn-primary btn-lg my-4">Edit friend's details</a>
            </form>
        </div>


        <div class="col-md-7 col-lg-8">
            <h3 class="pb-2">
                My friend's pets
                <a href="~/Edit/AddEditPet" class="btn btn-success btn-sm" type="button">Add</a>
            </h3>
            <div class="row mb-2 text-center">
                @foreach (var pet in Model.ViewFriend.Pets)
                {
                    <div class="col-md-10 themed-grid-col">
                        <span class="form-control">@pet.Name the @pet.Mood @pet.Kind </span>
                    </div>

                    <div class="col-md-2 themed-grid-col">
                        <a href="~/Edit/AddEditPet?id=@pet.PetId" class="btn btn-secondary btn-sm" type="button">Edit</a>
                    </div>
                }
            </div>
        </div>
     

        <div class="col-md-7 col-lg-8">
            <h3 class="pb-2">
                My friend's favorite quotes
                <a href="~/Edit/AddEditQuote" class="btn btn-success btn-sm" type="button">Add</a>
            </h3>
            <div class="row mb-2 text-center">
                @foreach (var quote in Model.ViewFriend.Quotes)
                {
                    <div class="col-md-10 themed-grid-col">
                        <span class="form-control">@quote.Quote 
                            <div class="col-md-16 text-end"><b>@quote.Author </b></div>
                        </span>
                    </div>
                
                    <div class="col-md-2 themed-grid-col">
                        <a href="~/Edit/AddEditQuote" class="btn btn-secondary btn-sm" type="button">Edit</a>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

            @*<input type="hidden" name="petId" data-seido-selected-item-id-target />

            <h3 class="pb-2">
                My friend's pets
                <a href="~/Edit/AddEditPet" class="btn btn-success btn-sm" type="button">Add</a>
            </h3>

            <div class="row mb-2 text-center">
                @if (Model.ViewFriendIM.PetIM != null && Model.ViewFriendIM.PetIM.Any())
                {

                    @foreach (var pet in Model.ViewFriendIM.PetIM)
                    {
                        <div class="mb-1">
                            <span class="form-control">@pet.Kind</span>
                        </div>
                        <div class="mb-3">
                            <span class="form-control">@pet.Mood</span>
                        </div>
                    }
                }
                else
                {
                    <div class="form-control mb-3">Has no Pets</div>
                }
                *@